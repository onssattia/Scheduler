{% extends 'base.html.twig' %}

{% block title %}Nouveau enseignant{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-xs-12">
            <h1>Créer un nouveau enseignant</h1>
            <hr/>
            {% include 'common/flash_message.html.twig' %}
            <div class="text-right margin-bottom-30">
                <a href="{{ path('teacher_index') }}">
                    Retour à la liste
                </a>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-8 col-lg-offset-2">
            {{ include('admin/teacher/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
